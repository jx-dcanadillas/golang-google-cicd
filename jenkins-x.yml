buildPack: go
pipelineConfig:
  pipelines:
    overrides:
    - name: make-build
      pipeline: release
      stage: build
      step:
        agent:
          image: golang:1.10
        command: go version && echo $GOPATH && pwd
