buildPack: go
pipelineConfig:
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        name: make-build 
        step:
          command: go test
          agent:
            image: golang:1.10

