buildPack: go
pipelineConfig:
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        name: make-build 
        agent:
          image: golang:1.10
        step:
          command: go test

